package javat;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class Test {
//    private String name = "abc";
    public static void main(String[] args) {
        /*Test test = new Test();
        Test testB = new Test();
        String result = test.equals(testB) + ",";//false,
        result += test.name.equals(testB.name) + ",";//false,true,
        result += test.name == testB.name;//false,true,true
        System.out.println(result);*/


       /* List<String> list = Arrays.asList("hello","world","stream");
        list.stream().map(item->item+item).collect(Collectors.toList()).forEach(System.out::println);

        System.out.println("list1======"+list);*/
        /*list.stream().map(item->item+item).collect(
                ArrayList::new,
                (list1,value )-> list1.add(value),
                (list1 ,list2)-> list1.addAll(list2)
        ).forEach(System.out::println);*/


        System.out.println("derewrew");
    }


}